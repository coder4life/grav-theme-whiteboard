<div class="list-group">
    {% for item in items %}
        {% if item.action %}
            {% include 'bootstrap/buttons.html.twig' with { 'text': item.text } %}
        {% elseif item.url %}
            <a href="{{ item.url }}" class="list-group-item">{{ item.menu }}</a>
        {% else %}
            <span class="list-group-item">{{ item.text }}</span>
        {% endif %}
    {% endfor %}
</div>