<section class="section-text text-center">
    <div class="container">
        <h1>{{ page.header.title}}</h1>
        {{ page.content }}
        {% if page.header.button.text %}
            <a class="btn btn-outline-primary" href="#" role="button">{{ page.header.button.text }}</a>
        {% endif %}
    </div>
</section>